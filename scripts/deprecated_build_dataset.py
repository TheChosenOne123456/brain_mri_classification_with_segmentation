"""DEPRECATED：旧的非 K-Fold 数据集构建入口。

实现可从 Git 历史追溯；当前项目只维护 scripts.build_dataset_kfold。
"""


if __name__ == "__main__":
    raise SystemExit(
        "This non-K-Fold script is deprecated. "
        "Use: python -m scripts.build_dataset_kfold --help"
    )
